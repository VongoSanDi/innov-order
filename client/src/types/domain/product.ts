/**
 * data formatted
 */
export interface FrontendProductData {
  abbreviated_product_name: string;
  allergens: string;
  brands: string;
  code: number;
  // Add keys i want to send to the vertical table here
}
